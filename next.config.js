/** @type {import('next').NextConfig} */
const nextConfig = {
  // Set the root directory to avoid lockfile warning
  // This tells Next.js where the project root is when multiple lockfiles are detected
};

module.exports = nextConfig;

